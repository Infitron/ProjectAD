name: .NET Core

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 2.2.104
    - name: Install dependencies
      working-directory: ./ProjectADApi/ProjectADApi
      run: dotnet restore
    - name: Build Blue
      working-directory: ./ProjectADApi/ProjectADApi
      run: dotnet build --configuration Release 
    - name: Publish Blue
      working-directory: ./ProjectADApi/ProjectADApi/ProjectADApi.csproj
      run: dotnet publish  -c Release -o projectad
    
